# This is a Databricks asset bundle definition for project_interface.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: project_interface
  uuid: a1e24605-80e9-4906-a479-e8717c40eb40

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1942110704800933.13.azuredatabricks.net
    variables:
      catalog: gabriel_dev_1942110704800933
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-1942110704800933.13.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/gabriel.rangel@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/gabriel.rangel@databricks.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: gabriel_dev_1942110704800933
      schema: prod
    permissions:
      - user_name: gabriel.rangel@databricks.com
        level: CAN_MANAGE
