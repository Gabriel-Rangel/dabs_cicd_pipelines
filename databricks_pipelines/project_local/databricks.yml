# This is a Databricks asset bundle definition for project_local.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: project_local
  uuid: a54820e0-70b3-4da2-9c2a-11ab1dc68bef

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1942110704800933.13.azuredatabricks.net
    variables:
      catalog: gabriel_dev_1942110704800933
      schema: dev
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-2909807607791205.5.azuredatabricks.net
      root_path: /Workspace/pipelines_etl/${bundle.name}/${bundle.target}
    variables:
      catalog: gabriel_prod_2909807607791205
      schema: prod
    permissions:
      - service_principal_name: 0be436dc-f9f2-45e2-925b-632ef8d5f47f
        level: CAN_MANAGE

